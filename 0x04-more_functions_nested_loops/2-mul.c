#include "main.h"
#include <ctype.h>

/**
 * mul - a function that multiplies two integers.
 * @a: first integer
 * @b: second integer
 * Return: the product of a andb
 */

int mul(int a, int b)
{
	return (a * b);
}
